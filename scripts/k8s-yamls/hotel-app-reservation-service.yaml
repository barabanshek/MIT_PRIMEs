apiVersion: v1
kind: Service
metadata:
  name: hotel-app-reservation
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: hotel-app-reservation
  ports:
  - port: 50000
    targetPort: 50000